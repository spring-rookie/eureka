server:
  port: 8761

eureka:
  serviceUrl:
    defaultZone: https://eureka-service-api.herokuapp.com/

spring:
  application:
    name: eureka-service

  profiles:
    active:
      dev

  cloud:
    config:
      uri: http://localhost:5555
